export interface KingdomInfo { banner_icon: number; color: string; ink: string }
export interface PersonInfo { asset_id: string; profession?: string; sex: string }
export interface World { description: string; name: string }
